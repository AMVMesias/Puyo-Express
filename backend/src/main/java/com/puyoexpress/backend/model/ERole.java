package com.puyoexpress.backend.model;

/**
 * Enum representing the available user roles in the system.
 * Each role maps to a specific frontend view and set of backend permissions.
 */
public enum ERole {
    ROLE_CUSTOMER,
    ROLE_RESTAURANT,
    ROLE_DRIVER
}
